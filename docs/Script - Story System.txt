 < Definition >
• 스크립트(script) : 순차적으로 실행되는 절차들의 모음.
 - script : n. 대본, 원고
• 브랜치(branch) : 순차적으로 실행되는 절차 배열.
• 페이지(page) : 각각의 절차. 실행될 함수(인터페이스)와 실행 시 참조할 정보를 담은 객체이다.
• 분기(watershed) : 다른 브랜치로 이동하는 경우의 수가 있는 페이지.
• 리더(Reader) : 페이지를 실행시킨다.
• 
• 

 « Script » 
• 기본 브랜치의 첫 페이지를 실행. 